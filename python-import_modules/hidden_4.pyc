#!/usr/bin/python3
import dis
import sys
if __name__ == "__main__":
    filename = '/tmp/hidden_4.pyc'
    with open(filename, 'rb') as f:
        bytecode = f.read()
    for name in dir(dis):
        if not name.startswith('__'):
            print(name)
